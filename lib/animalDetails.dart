import 'package:flutter/material.dart';

class animaldetails {
  final String image, animal, title, description, lifespan, speed;

  animaldetails(this.image, this.animal, this.title, this.description,
      this.lifespan, this.speed, this.id, this.color);

  final int id;
  Color color;
}
